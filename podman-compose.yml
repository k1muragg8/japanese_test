version: '3.8'

services:
  kana-tutor:
    container_name: kana-tutor
    build:
      context: .
      dockerfile: Containerfile
    ports:
      - "3000:3000"
    environment:
      - RUST_LOG=info
    restart: unless-stopped
    command: ["./kana-tutor", "--web"]